/**
 * Quizz on how to start a very simple node project :)
 *  - To install a new dependency, for example, "mathjs", run `npm install mathjs` in your terminal
 */

const myFile = require('./myFile.js');

console.log("Hello WOrld!" + myFile);